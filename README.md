```
# Run on source
cargo run
# With options
cargo run -- --length 20 --min-numeric 3 --exclude-ambiguous

# Build
cargo build --release
cp ./target/release/genpass ~/[your]/[favorite]/[location]/
genpass
```
